import { PlaceholderScreenFrame } from "../../navigation/PlaceholderScreenFrame";

export function TodayScreen() {
  return (
    <PlaceholderScreenFrame
      title="Today"
      message="Today screen wiring will land in the next wave. This placeholder locks the navigation contract and keeps the new shell mergeable."
      testID="today-screen-placeholder"
    />
  );
}
