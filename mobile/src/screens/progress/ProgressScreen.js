import { PlaceholderScreenFrame } from "../../navigation/PlaceholderScreenFrame";

export function ProgressScreen() {
  return (
    <PlaceholderScreenFrame
      title="Progress"
      message="Progress screen wiring will land in the next wave. This placeholder establishes the merged destination and its route name now."
      testID="progress-screen-placeholder"
    />
  );
}
