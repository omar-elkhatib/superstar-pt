import { PlaceholderScreenFrame } from "../../navigation/PlaceholderScreenFrame";

export function LogScreen() {
  return (
    <PlaceholderScreenFrame
      title="Log"
      message="Log screen wiring will land in the next wave. This placeholder keeps the bottom-tab shell stable while logging UI is rebuilt."
      testID="log-screen-placeholder"
    />
  );
}
