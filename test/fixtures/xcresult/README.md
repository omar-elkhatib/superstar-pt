# xcresult fixture

`UIFeedback-sample.xcresult` is a real Xcode-produced result bundle captured from this repo's iOS UI test flow on February 28, 2026.

It is intentionally small (~824KB) and used by integration tests to validate PNG attachment extraction against real `.xcresult` data.
